PKG_CPPFLAGS= -I../windows/gpgme-1.5.5/include
PKG_LIBS= -L../windows/gpgme-1.5.5/lib${R_ARCH} -lgpgme -lgpg-error -lassuan -lws2_32
PKG_CFLAGS= -DDEBUG

all: clean

clean:
	rm -f $(OBJECTS)
	rm -f gpg.dll
